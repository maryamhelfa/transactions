CREATE TABLE REMISE_ENTITY (
                               id BIGINT PRIMARY KEY,
                               montant_min DOUBLE,
                               montant_max DOUBLE,
                               taux DOUBLE
);