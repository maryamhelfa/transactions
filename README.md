# Transaction & Remise API

A Spring Boot REST API that manages transactions with discount (remise) logic.

## Endpoints
- `POST /transactions` — Create a transaction
- `GET /transactions/{id}` — Get a transaction by ID
- `DELETE /transactions/{id}` — Delete a transaction

## Tech Stack
- Java / Spring Boot
- JdbcTemplate (H2 / SQL)
- REST API
